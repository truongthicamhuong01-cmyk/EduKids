/* import { defineConfig } from "vite";

export default defineConfig({
  build: {
    outDir: "dist",
  },
});
 */
/* import { defineConfig } from "vite";

export default defineConfig({
  base: "./",
});
 */
import { defineConfig } from "vite";

export default defineConfig({
  base: "/",
});
